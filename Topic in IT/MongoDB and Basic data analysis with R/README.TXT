Datasets are loaded in to MongoDB server through CMD command in Windows 10.

R package is used to connect R working space with MongoDB and load data from MongoDB to R and processing. 

It works for the big data which requires NoSQL storage such as MongoDB.